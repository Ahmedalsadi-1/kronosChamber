# UI Theme System

## Mandatory Rule for UI Work
When working on UI components, styling, or visual design, study the `theme-system` skill first.

Use:

```txt
skill({ name: "theme-system" })
```

## Color and Token Requirements
- Use theme tokens for all colors.
- Do not hardcode colors.
- Do not use Tailwind color classes as substitutes for theme tokens.
